#include <iostream>
#include <cstring>
using namespace std;
class Dogovor;
class Potpisuvac
{
private:
    char ime[20];
    char prezime [20];
    char embg[14];
public:
    //copy constructor
    Potpisuvac (const Potpisuvac &p)
    {
        strcpy(ime,p.ime);
        strcpy(prezime,p.prezime);
        strcpy(embg, p.embg);
    }
    //default constructor
    Potpisuvac() {}
    //constructor with arguments
    Potpisuvac(char i[], char pr[], char mb[])
    {
        strcpy(ime,i);
        strcpy(prezime,pr);
        strcpy(embg, mb);
    }
    friend Dogovor;
};
class Dogovor
{
private:
    Potpisuvac potpisuvac[3];
    char kategorija[50];
    int brojDogovor;
public:
    Dogovor() {}
    Dogovor (int broj, char k[], Potpisuvac p[])
    {
        brojDogovor = broj;
        strcpy(kategorija,k);
        for(int i = 0; i < 3; i++)
        {
            potpisuvac[i] = p[i];
        }
    }
    bool istEMBG ()
    {
        for (int i  = 0; i < 3; i++)
        {
            for(int j = i+1; j < 3; j++)
            {
                 if (strcmp(potpisuvac[i].embg,potpisuvac[j].embg) == 0 )
                 {
                     return true;
                 }
            }

        }
        return false;
    }
};
int main ()
{
    char embg[14], ime[20], prezime[20], kategorija[20];
    int broj, n;
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        cin >> embg >> ime >> prezime;
        Potpisuvac p1(ime,prezime,embg);
        cin >> embg >> ime >> prezime;
        Potpisuvac p2(ime,prezime,embg);
        cin >> embg >> ime >> prezime;
        Potpisuvac p3(ime,prezime,embg);
        cin >> broj >> kategorija;
        Potpisuvac p[3];
        p[0] = p1;  p[1] = p2;  p[2] = p3;
        Dogovor d(broj, kategorija, p);
        cout << "Dogovor " << broj << ":" << endl;
        if(d.istEMBG() == true)
            cout << "Postojat potpishuvaci so ist EMBG" << endl;
        else
            cout << "Ne postojat potpishuvaci so ist EMBG" << endl;

    }
    return 0;
}
